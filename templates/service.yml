apiVersion: v1
kind: Service
metadata:
  name: kubeserve-active
spec:
  type: NodePort
  selector:
    app: kubeserve
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080

---
apiVersion: v1
kind: Service
metadata:
  name: kubeserve-preview
spec:
  type: NodePort
  selector:
    app: kubeserve
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30081
